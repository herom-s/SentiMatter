# CPU-only PyTorch: this project runs inference on CPU, so skip the ~2.5 GB CUDA/NVIDIA stack
--extra-index-url https://download.pytorch.org/whl/cpu

beautifulsoup4>=4.12.0
fastapi>=0.115.0
pydantic>=2.0.0
torch>=2.1.0
transformers>=4.36.0
uvicorn[standard]>=0.32.0
